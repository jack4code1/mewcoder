# MewCode - 产品规格说明

## 背景

终端 AI 编程助手市场存在一个空白：现有工具要么过于笨重（IDE 插件），要么功能单一（仅代码补全），要么缺乏真正的自主调试能力。开发者需要一个轻量、智能、能真正理解项目上下文并自主完成任务的终端工具。

## 目标用户

- 个人开发者
- 日常开发、代码审查、代码学习、自动化流程、原型开发场景

## 五层架构

```
┌─────────────────────────────────────────────────────────────┐
│  交互层 (TUI)                                               │
│  上方：对话区域，模型回复流式显示                            │
│  下方：输入框                                                │
│  底部：状态栏（模型、Token 用量等）                          │
├─────────────────────────────────────────────────────────────┤
│  引擎层 (Agent 基础设施)                                     │
│  LLM 客户端（多模型适配）                                    │
│  对话管理器（会话持久化、Token 管理）                        │
├─────────────────────────────────────────────────────────────┤
│  工具层                                                      │
│  文件读写、命令执行、代码搜索、Git 操作                      │
├─────────────────────────────────────────────────────────────┤
│  记忆层                                                      │
│  项目上下文、对话历史、用户偏好                              │
├─────────────────────────────────────────────────────────────┤
│  安全层                                                      │
│  命令确认、diff 预览、操作回滚、沙箱执行                      │
└─────────────────────────────────────────────────────────────┘
```

## 阶段目标

完成一个可以调用适配多种 LLM 和不同 API 协议的可以进行流式对话的 agent 工具，并且有初步的 TUI 界面，并且模块化编程后期可以便捷的增加/修改功能。

## 能力清单

### 交互层 (TUI)
- 使用 Textual 框架构建终端界面
- 上方对话区域，模型回复逐字符流式显示
- 下方输入框，支持历史命令、命令补全、多行粘贴、文件引用、内置命令
- 连按两次 Tab 自动优化提示词（与 / 命令补全不冲突）
- 底部状态栏可配置显示：当前模型、Token 用量、会话时长、工作目录、Agent 状态、对话模式
- 快捷键：Ctrl+C 中断、Ctrl+L 清屏、Ctrl+S 保存、Ctrl+O 切换模型、Ctrl+T 切换模式、F1/Ctrl+H 帮助
- 对话区域支持文本选择和复制
- 流式输出实时刷新，同一回复在同一行内显示
- 输入时无背景闪烁
- 支持 Markdown 格式渲染（代码高亮、标题、列表等）

### 引擎层
- LLM 客户端统一接口 + 适配器模式
- 支持 OpenAI API 格式
- 支持 Claude API 格式
- 支持 Ollama 本地 API 格式
- 支持自定义 API 端点和格式
- 自动检测并适配模型提供商
- SSE 流式传输
- 对话管理器：消息历史、Token 管理、上下文截断、持久化存储、对话分支、多会话管理
- Token 计数基于 API 返回值
- 会话持久化使用 YAML 格式
- API 调用失败自动重试（可配置次数）+ 提示用户

## 非功能要求

- 准确性优先于速度
- 支持所有编程语言（按文件扩展名识别）
- 模块化设计，便于扩展
- 自适应并行执行

## 设计骨架

```
mewcode/
├── pyproject.toml
├── src/
│   └── mewcode/
│       ├── __init__.py
│       ├── main.py
│       ├── cli.py
│       ├── engine/
│       │   ├── __init__.py
│       │   ├── models/
│       │   │   ├── __init__.py
│       │   │   ├── client.py          # LLM 客户端抽象基类
│       │   │   └── message.py         # 消息和响应数据模型
│       │   ├── adapters/
│       │   │   ├── __init__.py
│       │   │   ├── factory.py         # 适配器工厂
│       │   │   ├── openai_adapter.py
│       │   │   ├── claude_adapter.py
│       │   │   ├── ollama_adapter.py
│       │   │   └── custom_adapter.py
│       │   └── conversation.py        # 对话管理器
│       └── tui/
│           ├── __init__.py
│           ├── app.py                 # TUI 主应用
│           ├── widgets/
│           │   ├── chat_area.py       # 对话区域
│           │   ├── input_box.py       # 输入框
│           │   └── status_bar.py      # 状态栏
│           └── themes.py              # 主题配置
└── tests/
```

## Out of Scope

- IDE 插件形态（仅终端 CLI）
- 实时协作编辑
- 代码托管服务
- 可视化界面
- 移动端支持
- 企业级权限管理
- 代码部署功能
- 监控和告警功能
