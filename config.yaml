# MewCode 配置文件

# LLM 配置
llm:
  # 默认模型
  default_model: "mimo-v2.5-pro"
  default_provider: "custom"

  # 模型配置
  models:
    mimo-v2.5-pro:
      provider: "custom"
      base_url: "https://token-plan-cn.xiaomimimo.com/v1"
      api_key_env: "MIMO_API_KEY"
      api_key: "tp-ckg4u5c9o7dkg07yw1i72bogp7vtfljfm6gc34zkfn0fjw57"
      api_format: "openai"
      model: "mimo-v2.5-pro"

    gemma4-12b-it-qat:
      provider: "ollama"
      base_url: "http://localhost:11434"
      api_format: "openai"
      model: "gemma4-12b-it-qat"

# 会话配置
session:
  storage_dir: "~/.mewcode/sessions"
  auto_save: true

# TUI 配置
tui:
  theme: "default"
  show_token_usage: true
  show_session_duration: true

# 工具子系统(02-tools 章节新增)
tools:
  enabled: "all"           # "all" | "readonly" | [ReadFile, Glob, Grep, ...]
  bash_timeout: 30          # Bash 工具默认超时(秒),max 600
  max_output_chars: 10000   # 单个工具返回内容上限
