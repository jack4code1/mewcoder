# Isolated Task And Worktree Execution

Provide a controlled API for executing a task in a clean Git worktree, return
its diff and lifecycle state, and coordinate independent tasks with bounded
parallelism.
